#include <iostream>
using namespace std;

int main()
{
    string UserInput = "vikash kumar";
    // Push_back is used to single character append at the last of the string
    //  cout << UserInput<<endl;
    //  UserInput.push_back('U');
    //  cout << UserInput<<endl;

    // Pop_back is used to single character delete at the last of the string
    //  cout << UserInput << endl;
    //  UserInput.pop_back();
    //  cout << UserInput << endl;

    // Append is used to  Add the characters  at the last of the string
    // cout << UserInput << endl;
    // UserInput.append("sharma");
    // cout << UserInput << endl;

    // clear is used to Empty of the all string 
    cout << UserInput << endl;
    UserInput.clear();
    cout << UserInput << endl;

    return 0;
}